import { Flex } from "@chakra-ui/react";
import { memo, VFC } from "react";

export const Footer: VFC = memo(() => {
    return (
        <Flex bg="teal.200" maxW="95%" align="center" mx="auto">
            <>Footer</>
        </Flex>
    );
});
