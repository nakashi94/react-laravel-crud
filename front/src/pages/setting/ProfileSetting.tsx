import { Flex } from '@chakra-ui/react';
import { memo, VFC } from 'react';

export const ProfileSetting: VFC = memo(() => (
  <Flex align="center" maxW={{ base: '95%', md: '80%' }} mx="auto">
    <>ProfileSetting</>
  </Flex>
));
